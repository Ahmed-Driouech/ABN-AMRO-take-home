# PDF parsing -- hosted Unstructured partition API (hi_res).
# Free key: https://platform.unstructured.io
UNSTRUCTURED_API_KEY=
# Optional: your account's endpoint, if it differs from the default.
# UNSTRUCTURED_API_URL=https://api.unstructuredapp.io/general/v0/general

# Embeddings + generation.
OPENAI_API_KEY=
